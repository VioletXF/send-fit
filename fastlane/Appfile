app_identifier(ENV.fetch("APP_IDENTIFIER", "com.sendfit.app"))
apple_id(ENV["FASTLANE_USER"])
team_id(ENV["APPLE_DEVELOPMENT_TEAM"])

# Needed only for Apple Accounts that can access more than one App Store
# Connect provider. Leave unset for the account's default provider.
itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"]) if ENV["APP_STORE_CONNECT_TEAM_ID"] && !ENV["APP_STORE_CONNECT_TEAM_ID"].empty?
